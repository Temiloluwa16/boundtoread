const menubar = () => {
  return (
      <ul className="flex items-center justify-evenly absolute bottom-0 w-full bg-transparent">
        <li>Log In</li>
        <li>Sign Up</li>
        <li>Collection</li>
      </ul>
  );
};

export default menubar;
